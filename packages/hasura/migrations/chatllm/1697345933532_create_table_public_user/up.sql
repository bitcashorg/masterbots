CREATE TABLE "public"."user" ("user_id" serial NOT NULL, "username" text NOT NULL, "email" text NOT NULL, "password" text NOT NULL, "date_joined" timestamptz NOT NULL DEFAULT now(), "last_login" timestamptz DEFAULT now(), "profile_picture" text, PRIMARY KEY ("user_id") , UNIQUE ("user_id"), UNIQUE ("username"), UNIQUE ("email"));COMMENT ON TABLE "public"."user" IS E'Table storing information about registered users.';
