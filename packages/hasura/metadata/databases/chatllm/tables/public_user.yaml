table:
  name: user
  schema: public
array_relationships:
  - name: user_chatbot_preferences
    using:
      foreign_key_constraint_on:
        column: user_id
        table:
          name: user_chatbot_preference
          schema: public
