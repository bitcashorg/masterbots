CREATE TABLE "public"."tag_enum" ("name" varchar NOT NULL, "domain" varchar NOT NULL, "frequency" numeric NOT NULL, PRIMARY KEY ("name","domain") , FOREIGN KEY ("domain") REFERENCES "public"."domain_enum"("name") ON UPDATE restrict ON DELETE restrict);
