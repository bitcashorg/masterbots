table:
  name: referral
  schema: public
  object_relationships:
    - name: user
      using:
        foreign_key_constraint_on: user_id
    - name: referral
      using:
        foreign_key_constraint_on: referrer_id